apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasky-deployment
  labels:
    app: tasky-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tasky-orca
  template:
    metadata:
      labels:
        app: tasky-orca
    spec:
      serviceAccountName: tasky-sa
      containers:
      - name: tasky-orca
        image: csnow/csnow-tasky:1.2
        env:
        - name: MONGODB_URI
          value: "mongodb://adminuser:changeme@mongodb-0:27017"
        - name: SECRET_KEY
          value: "secret-change"
        command: ["/app/tasky"] 
