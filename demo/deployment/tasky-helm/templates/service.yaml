apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasky-helm.fullname" . }}
  labels:
    {{- include "tasky-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tasky-helm.selectorLabels" . | nindent 4 }}
